{
  "name": "voicefront",
  "private": true,
  "version": "1.0.0",
  "description": "White-label AI voice receptionist platform for clinics and construction companies",
  "engines": {
    "node": ">=18.18.0"
  },
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm run dev -w apps/api\" \"npm run dev -w apps/web\"",
    "build": "npm run build -w apps/api && npm run build -w apps/web",
    "typecheck": "npm run typecheck -w apps/api && npm run typecheck -w apps/web",
    "test": "npm run test -w apps/api",
    "db:setup": "npm run generate -w apps/api && npm run db:push -w apps/api && npm run db:seed -w apps/api",
    "db:migrate": "npm run db:migrate -w apps/api",
    "db:seed": "npm run db:seed -w apps/api"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
